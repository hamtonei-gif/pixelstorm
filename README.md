# PixelStorm

Hand gesture jigsaw puzzle game built with Python, OpenCV, and MediaPipe.

## Build
Push to main branch to automatically build the Windows .exe via GitHub Actions.
Download the artifact from the Actions tab after the build completes.
